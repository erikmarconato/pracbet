package com.pracbet.pracbet.Bet.enums;

public enum ResultBetEnum {
    Won(), // ganhou
    Lost(), // perdeu
    Void(), // aposta anulada
    Refunded() // recebeu o valor de volta (sem lucro ou prejuízo)
}
